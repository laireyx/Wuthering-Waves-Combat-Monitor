import { style } from '@vanilla-extract/css';

export const warningCardStyle = style({
  padding: 24,
});

export const warningTitleStyle = style({
  textAlign: 'center',
  color: '#440',
});

export const warningTextStyle = style({
  margin: 0,
  textAlign: 'center',
});
